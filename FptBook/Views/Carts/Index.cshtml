@model IEnumerable<FptBook.Models.Cart>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BookIsbn)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BookIsbn)
                </td>
                 <td>
                    @Html.DisplayFor(modelItem => item.Quantity)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
           
            </tr>
        }
    </tbody>
</table>
<a asp-controller="Home" asp-action="Checkout">Checkout</a>



@*@using FptBook.Models;
@model List<Cart>
<link href="~/Content/TrangChuCss.css" rel="stylesheet" />
@{
    ViewBag.Title = "Cart";
    Layout = "~/Views/LayOut/UserLayout.cshtml";
}
<div id="divGioHang">
    <table border="1" cellpadding="2" cellspacing="15" width="1000" height="400">
        <tr>
            <td>Book Name</td>
            <td>Cover Image</td>
            <td id="TenCot">Price</td>
            <td id="TenCot">Quantity</td>
            <td id="TenCot">Total</td>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Book</td>
                <td> href="@Url.Action("ViewDetail", "Book", new { @_BookID = item.BookIsbn}) width="100" height="150" </td>
                <td id="ChuSo">@string.Format("{0:0,0}", @item.Price) $</td>
                <td id="ChuSo">@item.Quantity</td>
                <td id="ChuSo">@string.Format("{0:0,0}", @item.Total) $</td>
            </tr>
        }
        <tr>
            <td colspan="6" id="ChuSo">
                Total: ("TotalCart", "Cart")
            </td>
        </tr>
        <tr>
            <td colspan="6" id="TenCot">
                @using(Html.BeginForm("Bill","Cart"))
                {
                    <input type="submit" style="background-color:green;color:white;font-size:12pt;cursor:pointer" value="Payment" />
                }
            </td>
        </tr>
    </table>
</div>
*@